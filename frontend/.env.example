# Copy to .env.local and adjust as needed.
# When using the Vite dev proxy (default), this can be left blank —
# the proxy forwards /api/* to localhost:6380 automatically.
# Set this explicitly when deploying the frontend to a remote host.
VITE_API_BASE=http://127.0.0.1:6380
